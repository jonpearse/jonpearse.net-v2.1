- cancel_link = params[:back_to] || { action: :index }

.form__buttons
  %button.btn.btn--primary.btn--large{ type: :submit }= prompt_t( @content, :save_button )
  = link_to prompt_t( @content, :cancel_button ), cancel_link, class: 'btn btn--large btn--secondary'
