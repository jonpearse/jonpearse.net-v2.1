:ruby

  # default the value
  value ||= nil

  # get an instance ID if appropriate
  id = value.nil? ? nil : value.id

.content-select__instance{ data: { instance: true, id: id }}
  %input{ type: :hidden, name: field[:instance], value: id }
  %span.content-select__label{ 'data-label': true }= (value.nil? ? '' : value.to_s )
